const sum = (n1, n2) => n1 + n2;
const sub = (n1, n2) => n1 - n2;
const mult = (n1, n2) => n1 * n2;
const div = (n1, n2) => n1 / n2;
const PI = 3.14;
const num = 12;


const obj = {
    sum: sum,
    sub: sub,
    mult: mult,
    div: div,
    PI: PI,
    num: num
};

module.exports = obj;
